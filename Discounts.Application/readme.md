Use Cases: It coordinates tasks and delegates work to the domain entities.
Services & Interfaces: It defines application-specific services and the interfaces for external dependencies.
DTOs (Data Transfer Objects): It defines the models used to move data between the API/Web layers and the internal services, ensuring the internal domain models aren't exposed directly.
Mapping: handles the transformation between Entities and DTOs.